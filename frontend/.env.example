# Base URL of the CWAC API.
#
# Required for any build that is not served from the same origin as the API.
# Without it a development build falls back to http://localhost:8000 and a
# production build falls back to a same-origin relative path.
VITE_API_BASE_URL=http://localhost:8000
